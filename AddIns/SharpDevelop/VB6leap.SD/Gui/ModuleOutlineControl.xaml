<UserControl x:Class="VB6leap.SDAddin.Gui.ModuleOutlineControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ToolBar Grid.Row="0" Margin="1">
            <Label Content="Sort:" FontWeight="Bold" />
            <RadioButton Content="Occ." IsChecked="True" />

            <Separator />
        </ToolBar>

        <TreeView Grid.Row="1" Margin="1" x:Name="trvLayout">

        </TreeView>
    </Grid>
</UserControl>
